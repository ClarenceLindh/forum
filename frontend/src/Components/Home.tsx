import React from "react";
import CreateThread from "./CreateThread";
import "../Styles/Home.scss";

const Home = () => {
  return (
    <div className="main">
      <CreateThread />
    </div>
  );
};

export default Home;
